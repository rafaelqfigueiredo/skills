# /fig:plan

Framework-aware feature planning — stress-tests the goal then produces a structured implementation plan using personal conventions for the detected stack.

## Usage

```
/fig:plan                    # Detect framework and plan current task
/fig:plan <goal>             # Plan a specific feature or change
```

Launch the `fig-planner` agent.
